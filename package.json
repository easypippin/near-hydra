{
  "name": "near-hydra-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Unofficial all-in-one CLI + MCP server for the full NEAR stack (accounts, Chain Signatures, NEAR Intents).",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/core packages/mcp-server packages/cli",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
    "mcp": "node packages/mcp-server/dist/index.js",
    "hydra": "node packages/cli/dist/index.js",
    "postinstall": "node scripts/postinstall.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}
